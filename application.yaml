spring:
   datasource:
      username: turtles
      password: turtles
      url: jdbc:mysql://localhost:3306/turtles
   
   jpa:
      hibernate:
         ddl-auto: none
      show-sql: true
      defer-database-initialization: = true
   sql:
      init:
         mode: never
        
    
   